<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <include resource="org/springframework/boot/logging/logback/defaults.xml" />
  <springProperty scope="context" name="logfilePath" source="slf4j.logging.file.path"/>
  <appender name="CONSOLE"
    class="ch.qos.logback.core.ConsoleAppender">
    <layout class="ch.qos.logback.classic.PatternLayout">
      <Pattern>
        %black(%d{ISO8601}) %highlight(%-5level) [%blue(%t)] %yellow(%C{1.}): %msg%n%throwable
      </Pattern>
    </layout>
  </appender>

  <appender name="FILE-ROLLING" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${logfilePath}/application.log</file>

    <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
      <fileNamePattern>${logfilePath}/archived/application.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
      <!-- each archived file, size max 10MB -->
      <maxFileSize>10MB</maxFileSize>
      <!-- total size of all archive files, if total size > 20GB, it will delete old archived file -->
      <totalSizeCap>20GB</totalSizeCap>
      <!-- 60 days to keep -->
      <maxHistory>60</maxHistory>
    </rollingPolicy>

    <encoder>
      <pattern>
        %d{ISO8601} %-5level %t  %C{1.}: %msg%n%throwable
      </pattern>
    </encoder>
  </appender>

  <springProfile name="default">
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.hibernate.validator" level="WARN"/>
    <logger name="org.apache" level="WARN"/>
    <logger name="com.cynosure" level="DEBUG"/>
    <root level="ERROR">
      <appender-ref ref="CONSOLE"/>
    </root>
  </springProfile>

  <springProfile name="qa">

    <logger name="org.springframework" level="WARN"/>
    <logger name="org.hibernate.validator" level="WARN"/>
    <logger name="org.apache" level="WARN"/>
    <logger name="com.cynosure" level="INFO"/>
    <root level="ERROR">
      <appender-ref ref="FILE-ROLLING" />
      <appender-ref ref="CONSOLE" />
    </root>
  </springProfile>

</configuration>